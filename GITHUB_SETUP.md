# Подготовка проекта для GitHub

Этот файл содержит инструкции по подготовке проекта для публикации на GitHub.

## ⚠️ ВАЖНО: Безопасность перед публикацией

### 1. Проверка секретных данных

Перед публикацией на GitHub необходимо убедиться, что следующие данные НЕ попадут в репозиторий:

- ✅ **SECRET_KEY** Django - должен быть в `.env` файле
- ✅ **База данных** - `db.sqlite3` уже в `.gitignore`
- ✅ **Медиа файлы** - папка `/media` уже в `.gitignore`
- ✅ **Файлы окружения** - `.env` файлы должны быть в `.gitignore`

### 2. Обновление settings.py

**КРИТИЧНО**: Перед публикацией необходимо:

1. Удалить хардкод SECRET_KEY из `waste_classification/settings.py`
2. Использовать переменные окружения для:
   - `SECRET_KEY`
   - `DEBUG`
   - `ALLOWED_HOSTS`

Пример изменений в `settings.py`:

```python
import os
from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.environ.get('SECRET_KEY', 'django-insecure-change-me-in-production')

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ.get('DEBUG', 'False') == 'True'

ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', '').split(',') if os.environ.get('ALLOWED_HOSTS') else []
```

### 3. Создание .env.example

Создайте файл `.env.example` с примером переменных окружения (без реальных значений):

```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
```

Этот файл можно безопасно коммитить в репозиторий.

### 4. Проверка .gitignore

Убедитесь, что `.gitignore` содержит следующие записи:

```
# Environment variables
.env
.env.local
.env.*.local
*.env

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/media
/staticfiles

# Python
__pycache__/
*.py[cod]
*.so
.Python
env/
venv/
ENV/

# Model files
*.h5
*.hdf5
*.pkl
*.joblib
```

### 5. Проверка перед коммитом

Перед первым коммитом выполните:

```bash
# Проверьте, что секретные файлы не попадут в репозиторий
git status

# Убедитесь, что .env файл не отслеживается
git check-ignore .env

# Если .env файл показывается в git status, добавьте его явно:
git rm --cached .env  # если он уже был добавлен
```

### 6. Создание README.md

Убедитесь, что `README.md` содержит:
- Описание проекта
- Инструкции по установке
- Информацию о переменных окружения
- Пример настройки `.env` файла

### 7. Финальная проверка

Перед пушем в GitHub:

```bash
# Проверьте, что нет секретных данных в коде
grep -r "SECRET_KEY" --include="*.py" | grep -v ".env"
grep -r "password" --include="*.py" | grep -v ".env"
grep -r "api_key" --include="*.py" | grep -v ".env"

# Проверьте размер файлов (большие файлы могут быть проблемой)
find . -type f -size +10M -not -path "./.git/*"
```

### 8. Первый коммит

```bash
# Инициализация репозитория (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Проверка статуса
git status

# Создание первого коммита
git commit -m "Initial commit: Waste Classification Django Project"

# Добавление remote репозитория (замените на ваш URL)
git remote add origin https://github.com/yourusername/your-repo-name.git

# Пуш в GitHub
git branch -M main
git push -u origin main
```

## 📝 Чеклист перед публикацией

- [ ] SECRET_KEY вынесен в переменные окружения
- [ ] DEBUG настроен через переменные окружения
- [ ] ALLOWED_HOSTS настроен через переменные окружения
- [ ] Создан `.env.example` файл
- [ ] `.env` файл добавлен в `.gitignore`
- [ ] `db.sqlite3` не попадет в репозиторий
- [ ] Медиа файлы не попадут в репозиторий
- [ ] Проверены все файлы на наличие секретных данных
- [ ] README.md обновлен с инструкциями
- [ ] requirements.txt актуален

## 🔒 Дополнительные рекомендации

1. **Используйте GitHub Secrets** для CI/CD (если планируете использовать)
2. **Не коммитьте** файлы с реальными данными пользователей
3. **Используйте** `.gitkeep` для пустых директорий, которые должны быть в репозитории
4. **Добавьте** лицензию, если планируете открытый исходный код

## ⚠️ Если вы случайно закоммитили секреты

Если вы случайно закоммитили секретные данные:

1. **Немедленно** измените все секретные ключи
2. Используйте `git filter-branch` или `git filter-repo` для удаления из истории
3. Или создайте новый репозиторий и начните заново

---

## 📦 После публикации на GitHub

После того, как проект опубликован на GitHub, вы можете использовать автоматический скрипт деплоя `deploy.sh` для установки проекта на сервер.

См. файл `DEPLOY_GCP.md` для полной инструкции по деплою на Google Cloud Platform.

---

**Готово!** После выполнения всех шагов проект готов к публикации на GitHub.

