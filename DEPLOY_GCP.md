# Полная инструкция по деплою на Google Cloud Platform

Этот файл содержит пошаговую инструкцию по деплою проекта на GCP VM с доменом `wasteclfmodel.kz`.

## 📋 Предварительные требования

1. **Google Cloud Platform аккаунт** с созданным проектом
2. **VM инстанс** в GCP (рекомендуется: Ubuntu 20.04 или 22.04 LTS)
3. **Домен** `wasteclfmodel.kz` настроенный и указывающий на IP вашего VM
4. **Проект загружен на GitHub**

## 🚀 Шаг 1: Создание VM инстанса в GCP

### 1.1 Создание VM

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект или создайте новый
3. Перейдите в **Compute Engine** → **VM instances**
4. Нажмите **Create Instance**

### 1.2 Настройки VM

Рекомендуемые параметры:
- **Name**: `wasteclfmodel-vm`
- **Region**: Выберите ближайший регион
- **Machine type**: `e2-medium` (2 vCPU, 4 GB RAM) или выше
- **Boot disk**: Ubuntu 22.04 LTS, размер минимум 20 GB
- **Firewall**: Разрешить HTTP и HTTPS трафик
- **Network tags**: `http-server`, `https-server`

### 1.3 Настройка файрвола

В разделе **VPC network** → **Firewall** убедитесь, что есть правила:
- `default-allow-http` (порт 80)
- `default-allow-https` (порт 443)
- `default-allow-ssh` (порт 22)

## 🌐 Шаг 2: Настройка DNS для домена

### 2.1 Получение внешнего IP адреса

1. В GCP Console найдите ваш VM инстанс
2. Скопируйте **External IP** адрес

### 2.2 Настройка DNS записей

В панели управления вашего доменного регистратора создайте A-запись:

```
Type: A
Name: @ (или пусто)
Value: <ваш-external-ip-адрес>
TTL: 3600
```

И для www поддомена:

```
Type: A
Name: www
Value: <ваш-external-ip-адрес>
TTL: 3600
```

**Важно**: DNS изменения могут занять от нескольких минут до 48 часов. Проверьте:

```bash
# Проверка DNS
nslookup wasteclfmodel.kz
ping wasteclfmodel.kz
```

## 🔐 Шаг 3: Подключение к VM

### 3.1 Через SSH в браузере

1. В GCP Console найдите ваш VM
2. Нажмите **SSH** → откроется терминал в браузере

### 3.2 Через локальный терминал (опционально)

```bash
# Если у вас настроен SSH ключ
ssh -i ~/.ssh/your_key username@<external-ip>
```

## 📥 Шаг 4: Загрузка проекта на сервер

### 4.1 Клонирование из GitHub

```bash
# Обновление системы
sudo apt-get update
sudo apt-get upgrade -y

# Установка Git (если не установлен)
sudo apt-get install -y git

# Клонирование проекта
cd /var/www
sudo git clone https://github.com/yourusername/your-repo-name.git wasteclfmodel
sudo chown -R $USER:$USER wasteclfmodel
cd wasteclfmodel
```

**Или** загрузите проект вручную через `scp`:

```bash
# С вашего локального компьютера
scp -r /path/to/project username@<external-ip>:/var/www/wasteclfmodel
```

## 🚀 Шаг 5: Запуск скрипта деплоя

### 5.1 Сделать скрипт исполняемым

```bash
cd /var/www/wasteclfmodel
chmod +x deploy.sh
```

### 5.2 Запуск скрипта

```bash
sudo bash deploy.sh
```

Скрипт автоматически выполнит:
- ✅ Установку всех системных зависимостей
- ✅ Установку Python и зависимостей проекта
- ✅ Настройку Django для production
- ✅ Настройку Gunicorn
- ✅ Настройку Nginx
- ✅ Получение SSL сертификата (если DNS настроен)
- ✅ Запуск всех сервисов

### 5.3 Что делает скрипт

1. **Обновление системы** - обновляет пакеты Ubuntu
2. **Установка зависимостей** - Python, Nginx, Certbot и др.
3. **Создание пользователя** - создает пользователя `django` для безопасности
4. **Настройка проекта** - клонирует/настраивает проект
5. **Виртуальное окружение** - создает venv и устанавливает зависимости
6. **Переменные окружения** - создает `.env` файл с настройками
7. **Обновление settings.py** - настраивает для production
8. **Миграции** - применяет миграции базы данных
9. **Статические файлы** - собирает статику Django
10. **Gunicorn** - настраивает WSGI сервер
11. **Nginx** - настраивает веб-сервер
12. **SSL** - получает Let's Encrypt сертификат
13. **Запуск сервисов** - запускает все необходимое

## ⚙️ Шаг 6: Ручная настройка (если нужно)

### 6.1 Создание суперпользователя Django

```bash
sudo -u django bash -c 'cd /var/www/wasteclfmodel && source venv/bin/activate && python manage.py createsuperuser'
```

### 6.2 Проверка работы сервисов

```bash
# Статус Gunicorn
sudo systemctl status wasteclfmodel

# Статус Nginx
sudo systemctl status nginx

# Логи Gunicorn
sudo journalctl -u wasteclfmodel -f

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### 6.3 Настройка .env файла (если нужно изменить)

```bash
sudo nano /var/www/wasteclfmodel/.env
```

Основные переменные:
```env
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=wasteclfmodel.kz,www.wasteclfmodel.kz,localhost,127.0.0.1
```

После изменения перезапустите Gunicorn:
```bash
sudo systemctl restart wasteclfmodel
```

## 🔒 Шаг 7: Настройка SSL (если не настроен автоматически)

Если SSL сертификат не был получен автоматически:

```bash
sudo certbot --nginx -d wasteclfmodel.kz -d www.wasteclfmodel.kz
```

Следуйте инструкциям на экране. Certbot автоматически обновит конфигурацию Nginx.

## 🔄 Шаг 8: Обновление проекта

Когда вы обновляете код на GitHub:

```bash
# Подключитесь к серверу
cd /var/www/wasteclfmodel

# Обновите код
sudo -u django git pull origin main

# Активируйте виртуальное окружение и обновите зависимости
sudo -u django bash -c 'source venv/bin/activate && pip install -r requirements.txt'

# Примените миграции (если есть новые)
sudo -u django bash -c 'source venv/bin/activate && python manage.py migrate'

# Соберите статику (если изменилась)
sudo -u django bash -c 'source venv/bin/activate && python manage.py collectstatic --noinput'

# Перезапустите Gunicorn
sudo systemctl restart wasteclfmodel
```

## 🛠️ Управление сервисами

### Перезапуск сервисов

```bash
# Перезапуск Gunicorn
sudo systemctl restart wasteclfmodel

# Перезапуск Nginx
sudo systemctl restart nginx

# Перезапуск обоих
sudo systemctl restart wasteclfmodel nginx
```

### Просмотр логов

```bash
# Логи Gunicorn
sudo journalctl -u wasteclfmodel -f

# Логи Nginx (ошибки)
sudo tail -f /var/log/nginx/error.log

# Логи Nginx (доступ)
sudo tail -f /var/log/nginx/access.log
```

### Проверка статуса

```bash
# Статус всех сервисов
sudo systemctl status wasteclfmodel
sudo systemctl status nginx
```

## 🐛 Решение проблем

### Проблема: Сайт не открывается

1. Проверьте статус сервисов:
   ```bash
   sudo systemctl status wasteclfmodel
   sudo systemctl status nginx
   ```

2. Проверьте логи:
   ```bash
   sudo journalctl -u wasteclfmodel -n 50
   sudo tail -n 50 /var/log/nginx/error.log
   ```

3. Проверьте файрвол GCP:
   - Убедитесь, что правила для HTTP/HTTPS активны
   - Проверьте теги сети на VM

4. Проверьте DNS:
   ```bash
   nslookup wasteclfmodel.kz
   ```

### Проблема: 502 Bad Gateway

Обычно означает, что Nginx не может подключиться к Gunicorn:

1. Проверьте, что Gunicorn запущен:
   ```bash
   sudo systemctl status wasteclfmodel
   ```

2. Проверьте права доступа к сокету:
   ```bash
   ls -la /run/gunicorn.sock
   ```

3. Перезапустите Gunicorn:
   ```bash
   sudo systemctl restart wasteclfmodel
   ```

### Проблема: Статические файлы не загружаются

1. Убедитесь, что статика собрана:
   ```bash
   sudo -u django bash -c 'cd /var/www/wasteclfmodel && source venv/bin/activate && python manage.py collectstatic --noinput'
   ```

2. Проверьте права доступа:
   ```bash
   sudo chown -R django:django /var/www/wasteclfmodel/staticfiles
   ```

3. Проверьте конфигурацию Nginx:
   ```bash
   sudo nginx -t
   ```

### Проблема: SSL сертификат не получен

1. Убедитесь, что DNS настроен правильно:
   ```bash
   nslookup wasteclfmodel.kz
   ```

2. Убедитесь, что порты 80 и 443 открыты в GCP

3. Попробуйте получить сертификат вручную:
   ```bash
   sudo certbot --nginx -d wasteclfmodel.kz -d www.wasteclfmodel.kz
   ```

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# Использование CPU и памяти
htop

# Использование диска
df -h

# Использование памяти
free -h
```

### Автоматическое обновление SSL

Certbot автоматически обновляет сертификаты. Проверьте cron задачу:

```bash
sudo certbot renew --dry-run
```

## 🔐 Безопасность

### Рекомендации:

1. **Регулярно обновляйте систему**:
   ```bash
   sudo apt-get update && sudo apt-get upgrade -y
   ```

2. **Используйте сильные пароли** для суперпользователя Django

3. **Настройте автоматические бэкапы** базы данных

4. **Ограничьте SSH доступ** только с определенных IP (опционально)

5. **Регулярно проверяйте логи** на подозрительную активность

## 📝 Полезные команды

```bash
# Перезапуск всего стека
sudo systemctl restart wasteclfmodel nginx

# Проверка конфигурации Nginx
sudo nginx -t

# Перезагрузка конфигурации Nginx
sudo nginx -s reload

# Создание бэкапа базы данных
sudo -u django bash -c 'cd /var/www/wasteclfmodel && source venv/bin/activate && python manage.py dumpdata > backup.json'

# Восстановление из бэкапа
sudo -u django bash -c 'cd /var/www/wasteclfmodel && source venv/bin/activate && python manage.py loaddata backup.json'
```

## ✅ Чеклист после деплоя

- [ ] Сайт открывается по адресу `http://wasteclfmodel.kz`
- [ ] SSL сертификат установлен и работает (`https://wasteclfmodel.kz`)
- [ ] Статические файлы загружаются (CSS, JS, изображения)
- [ ] Медиа файлы загружаются и отображаются
- [ ] Админ панель Django доступна (`/admin`)
- [ ] Создан суперпользователь Django
- [ ] Все сервисы запущены и работают
- [ ] Логи не показывают ошибок

---

**Готово!** Ваш проект должен быть доступен в интернете по адресу `https://wasteclfmodel.kz`

